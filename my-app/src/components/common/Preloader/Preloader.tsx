import React from 'react';
import preloader from "../../../assets/gif/oval.svg";

const Preloader = () => {
    return (
        <div>
            <img src={preloader} />
        </div>
    );
};

export default Preloader;